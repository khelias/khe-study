import type { GameConfig } from '../../types/game';
import { THEME } from '../themes';

export const SYLLABLE_BUILDER_CONFIG: GameConfig = {
  id: 'syllable_builder',
  title: 'SYLLABLE MASTER',
  theme: THEME.orange!,
  icon: 'Layers',
  emoji: '📖',
  desc: 'Put syllables together into a word',
  difficulty: 'easy',
  category: 'language',
};
