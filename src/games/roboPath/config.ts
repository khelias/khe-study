import type { GameConfig } from '../../types/game';
import { THEME } from '../themes';

export const ROBO_PATH_CONFIG: GameConfig = {
  id: 'robo_path',
  title: 'ROBO PATH',
  theme: THEME.indigo!,
  icon: 'Bot',
  emoji: '🤖',
  desc: 'Program the robot',
  difficulty: 'medium',
  category: 'logic',
};
